name: US Stock Data Update

on:
  schedule:
    # 日本時間 07:00 (UTC 22:00)
    - cron: '0 22 * * *'
    # 日本時間 23:30 (UTC 14:30)
    - cron: '30 14 * * *'
  workflow_dispatch:

# 書き込み権限を明示的に許可（exit code 128 対策）
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # v3からv4にアップグレード
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5 # v4からv5にアップグレード
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas
          
      - name: Run Scraper
        run: python src/scraper.py
        
      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git pull origin main --rebase
          
          git add data/stock_data.csv
          
          if ! git diff --cached --exit-code --quiet; then
            git commit -m "自動更新: $(date +'%Y-%m-%d %H:%M:%S') (JST)"
            git push origin main
          else
            echo "変更がないためスキップします"
          fi
